"use strict";
//alterando o tsconfig.json o typescript pode gerar aquivo .js compatível com a versão antiga.
let buttonTeste = document.getElementById('button');
button === null || button === void 0 ? void 0 : button.addEventListener('click', () => {
    console.log('funcionou o botao');
});
